"use client";
import { useEffect,useState } from "react";
type Settings={whatsapp:string;bookTitle:string;bookPrice:string;purchaseUrl:string};
export default function Admin(){const[data,setData]=useState<Settings>({whatsapp:"",bookTitle:"",bookPrice:"",purchaseUrl:""});const[pin,setPin]=useState("");const[allowed,setAllowed]=useState(false);const[status,setStatus]=useState("");useEffect(()=>{fetch("/api/settings").then(r=>r.json()).then(setData)},[]);async function save(){const r=await fetch("/api/settings",{method:"PUT",headers:{"content-type":"application/json"},body:JSON.stringify(data)});setStatus(r.ok?"Configurações publicadas no site.":"Não foi possível salvar.")}
return <main className="site-admin-page">{!allowed?<section className="admin-login"><span className="brand-mark">MV</span><p>ÁREA RESTRITA</p><h1>Gestão do site</h1><input type="password" value={pin} onChange={e=>setPin(e.target.value)} placeholder="Digite o PIN"/><button className="primary" onClick={()=>pin==="2026"?setAllowed(true):setStatus("PIN incorreto.")}>Entrar</button>{status&&<small>{status}</small>}</section>:<section className="admin-dashboard"><header><div><p>MATHEUS VIDAL · ADMINISTRAÇÃO</p><h1>Contato e livro</h1></div><a href="/">Ver site ↗</a></header><div className="admin-form"><label>WhatsApp com DDI e DDD<input value={data.whatsapp} onChange={e=>setData({...data,whatsapp:e.target.value})} placeholder="5511999999999"/><small>Este número receberá os pedidos de agenda.</small></label><label>Nome do livro<input value={data.bookTitle} onChange={e=>setData({...data,bookTitle:e.target.value})}/></label><label>Preço do livro<input value={data.bookPrice} onChange={e=>setData({...data,bookPrice:e.target.value})}/></label><label>Link de pagamento futuro<input value={data.purchaseUrl} onChange={e=>setData({...data,purchaseUrl:e.target.value})} placeholder="Opcional"/></label><button className="primary" onClick={save}>Salvar alterações</button>{status&&<p className="save-status">{status}</p>}</div></section>}</main>}
